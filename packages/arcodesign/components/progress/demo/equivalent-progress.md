## 设置等值进度（以10%数值递增） @en{Set equivalent progress (in 10% increments)}

#### 8

```js
import { Progress } from '@arco-design/mobile-react';

export default function ProgressDemo() {
    return (
        <Progress percentage={60} step={10} />
    );
}
```
