import Transition from './Transition.vue';
import { installFactory } from '../_helpers/install';

const install = installFactory(Transition);

export { Transition };

export default {
    install,
};
